/*#include "I2CHack.h"

bool I2CHack::Read(UINT8 count,UINT8* buffer)
{
	return Transaction(NULL,0,buffer,count);
}

bool I2CHack::Write(UINT8* data)
{
	return Transaction(data,sizeof(data),NULL, 0);
}*/
